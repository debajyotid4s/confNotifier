__pycache__/
*.py[cod]
.pytest_cache/
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
.DS_Store
*.log
selenium.log
geckodriver.log
chromedriver.log

# Testing / diagnostic files
classify_failures.py
corrected_classifications.json
diagnose_failures.py
diagnostic_results.json
test_homepage_fix.py
test.py

# Local documentation
PROJECT_STATE.md
ARTICLE_OUTLINE.md
REPORT_gemini_rate_limits.md
application_plan.md
go_cli_plan.md

# Database migrations are version-controlled: CI builds the whole schema from
# them on every push and the tests verify idempotency.
# (One-off diagnostic SQL still belongs in /tmp, not here.)

# Sensitive information — never commit keys, tokens, or credentials
.env
.env.*
*.pem
*.key
*.p12
*.jks
*.keystore
keystore.properties
secrets*
credentials*
service-account*.json

# Go migration
go-migration/

# Android — Call4Paper
Call4Paper/.gradle/
Call4Paper/build/
Call4Paper/app/build/
Call4Paper/.idea/
Call4Paper/*.iml
Call4Paper/app/*.iml
Call4Paper/local.properties
Call4Paper/app/google-services.json
Call4Paper/.kotlin/
Call4Paper/captures/

# API — FastAPI
api/__pycache__/
api/.venv/
api/venv/

# Gradle cache (root)
.gradle/

# Skills / local AI state (not part of repo)
Call4Paper/.agents/
Call4Paper/skills-lock.json
skills-lock.json

# Reports (local only)
Project_Report.pdf
Project_Report.tex

# Local tooling state
.opencode/

# Source assets — not needed in repo
Call4Paper/C4P_logo_source.png
Call4Paper/conference_tracker_android_architecture-1.md

# ML data collection — local dataset, not versioned (separate Neon tables, manual review)
data_collection/
